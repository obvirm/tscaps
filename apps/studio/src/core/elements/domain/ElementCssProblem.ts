/**
 * Something wrong with the CSS written against one element. The message
 * is addressed to whoever typed it: it names what will not work and why,
 * in the vocabulary of the text on screen.
 */
export interface ElementCssProblem {
  readonly message: string;
}
