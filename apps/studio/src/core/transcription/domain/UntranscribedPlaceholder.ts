/**
 * Caption text inserted over an audio region the local transcription
 * left empty. It is an ordinary editable word in the document; the
 * bracket style marks it as a meta annotation rather than speech.
 */
export const UNTRANSCRIBED_PLACEHOLDER_TEXT = '[inaudible]';
